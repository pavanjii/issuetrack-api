{
  "name": "issuetrack-api",
  "version": "1.0.0",
  "description": "REST API for tracking and triaging defects/issues across services, with filtering by status, priority, and affected service.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "supertest": "^7.2.2"
  },
  "license": "MIT"
}
